apiVersion: v1
kind: Pod
metadata:
  name: posts
spec:
  containers:
    - name: posts
      image: posts:1.0
      resources:
        limits:
          memory: "200Mi"
          cpu: "0.6"
        requests:
          memory: "100Mi"
          cpu: "0.3"
